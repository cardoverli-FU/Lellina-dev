# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Local-only dev launcher (sandbox process reaper workaround)
start-dev.sh
# Preview screenshots (sandbox-only artifacts)
preview-*.png
# Skills directory
/skills/

# Tool call artifacts (junk from agent tool runs)
/tool-results/
# Old git backup (NEVER commit)
.git.old/

# ─── SANDBOX-ONLY FILES (Agent Rule #9 — NEVER push to GitHub) ───
# These exist only in the dev sandbox, not part of the Lellina project.
/.zscripts/
/download/
/examples/
/mini-services/*
!/mini-services/chat-service/
/tests/
Caddyfile
# Top-level db/ is a sandbox SQLite runtime file. Project DB lives at src/lib/db/
/db/
# Any stray SQLite database files
*.db
*.db-journal
*.sqlite
*.sqlite-journal
# Root-level sandbox screenshots (public/ PNGs are tracked separately)
/*.png
/*.jpg
/*.jpeg

# Worklog — committed so fresh-cloned agents can read the build history.
# (See docs/phases.md "Agent Rules" — next-phase prompt reads worklog.md at repo root.)
# If you re-add sandbox noise here, sanitize it first (no tokens, no .env values).
# worklog.md

# Bun lockfile (sandbox-only — Render uses package-lock.json)
bun.lock

# Next-phase prompt (sandbox-only — contains API keys, tokens, passwords)
NEXT-PHASE-PROMPT.md
